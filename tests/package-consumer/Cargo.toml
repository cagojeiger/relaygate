[package]
name = "relaygate-package-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
relaygate-destination = "=0.5.3"
relaygate-protocol = "=0.5.3"
relaygate-sdk = "=0.5.3"
relaygate-token-issuer = "=0.5.3"
relaygate-transport = "=0.5.3"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
