[package]
name = "relaygate-package-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
relaygate-destination = "=0.5.2"
relaygate-protocol = "=0.5.2"
relaygate-sdk = "=0.5.2"
relaygate-token-issuer = "=0.5.2"
relaygate-transport = "=0.5.2"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
